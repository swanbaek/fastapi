server {
    listen 80;
    server_name myswan.shop www.myswan.shop 3.36.113.67;
   
    # FastAPI Static Files (/ 보다 위에 있어야 함!)
    location ^~ /static/ {
        proxy_pass http://127.0.0.1:8000/static/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # React 정적 파일
    location / {
        root /var/www/react_build;
        index index.html;
        try_files $uri /index.html;
    }
   
    # FastAPI - API
    location /api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    # FastAPI - Posts
    location ^~ /posts {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Content-Type $content_type;
        proxy_pass_request_body on;
        proxy_pass_request_headers on;
    }
    # FastAPI - Auth
    location /auth/ {
        proxy_pass http://127.0.0.1:8000/auth/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
===============================================
Cerbot설치후 아래와 같이 default파일 자동 변경됨
---------------------
server {
    server_name myswan.shop www.myswan.shop 3.36.113.67;

    # FastAPI Static Files (/ 보다 위에 있어야 함!)
    location ^~ /static/ {
        proxy_pass http://127.0.0.1:8000/static/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    # React 정적 파일
    location / {
        root /var/www/react_build;
        index index.html;
        try_files $uri /index.html;
    }

    # FastAPI - API
    location /api/ {
        proxy_pass http://127.0.0.1:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    # FastAPI - Posts
   location ^~ /posts {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Content-Type $content_type;
    proxy_pass_request_body on;
    proxy_pass_request_headers on;
  }
    # FastAPI - Auth
    location /auth/ {
        proxy_pass http://127.0.0.1:8000/auth/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/myswan.shop/fullchain.pem; # managed by Certbot
    ssl_certificate_key /etc/letsencrypt/live/myswan.shop/privkey.pem; # managed by Certbot
    include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}
server {
    if ($host = www.myswan.shop) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    if ($host = myswan.shop) {
        return 301 https://$host$request_uri;
    } # managed by Certbot
    listen 80;
    server_name myswan.shop www.myswan.shop 3.36.113.67;
    return 404; # managed by Certbot
}