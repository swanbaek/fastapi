
app/
 ├── api/          # 라우터(엔드포인트) 모음
 ├── core/         # 설정, 환경변수, 보안, CORS, init 관련, DB연결함수
 ├── crud/         # DB CRUD 함수 모음
 ├── models/       # SQLAlchemy 모델
 ├── schemas/      # Pydantic 스키마
 ├── services/     # 비즈니스 로직 (서비스 계층)
 ├── static/       # CSS, JS, 이미지 (정적 파일)
 ├── templates/    # Jinja2 HTML 템플릿
 ├── deps.py       # 의존성(dependencies)
 └── main.py       # FastAPI 진입점
----------------------------------------

MySQL의 scott 사용자와 eduDB 연결을 위한 환경설정이 완료.

.env에 DB_URL이 추가되어, scott 계정으로 eduDB에 연결.
database.py에서 환경변수 기반으로 SQLAlchemy 연결을 생성.
deps.py에 DB 세션 의존성 함수가 추가되어 FastAPI에서 DB 접근이 가능합니다.
config.py에서 환경변수 로드 및 DB_URL getter 함수가 추가되었습니다.
------------------------------------------
SQLAlchemy

클라이언트 요청 →  get_db() 실행
                  ├─ db = SessionLocal()        (세션 열기)
                  ├─ yield db  ───────────────► (라우터로 db 전달)
라우터 실행 끝 ◄─────── (다시 돌아옴)
                  └─ finally: db.close()        (세션 닫기)
