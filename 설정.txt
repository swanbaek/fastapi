
MySQL의 scott 사용자와 eduDB 연결을 위한 환경설정이 완료되었습니다.

.env에 DB_URL이 추가되어, scott 계정으로 eduDB에 연결합니다.
database.py에서 환경변수 기반으로 SQLAlchemy 연결을 생성합니다.
deps.py에 DB 세션 의존성 함수가 추가되어 FastAPI에서 DB 접근이 가능합니다.
config.py에서 환경변수 로드 및 DB_URL getter 함수가 추가되었습니다.